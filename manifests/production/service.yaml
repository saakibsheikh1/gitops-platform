apiVersion: v1
kind: Service
metadata:
  name: production-nginx-service
  namespace: production
spec:
  selector:
    app: production-nginx
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP